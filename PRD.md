# 유실물 촬영·편집 웹앱 구현 계획

## Context

유실물 접수 담당자가 습득물을 등록사이트에 올리기 전, 사진을 찍고 정리하는 과정을 하나의 도구로 해결하려는 것이 목적이다. 현재는 사진을 찍고 → 다른 프로그램에서 배경을 지우고 → 또 다른 프로그램에서 개인정보를 가리고 → 이미지 편집기에서 여러 장을 합치는 식으로 흩어져 있다.

필요한 것은 세 가지다.
1. 피사체(유실물) 외 배경 제거
2. 유실물에 찍힌 개인정보(이름, 주소, 카드번호 등) 블러 처리
3. 여러 각도에서 찍은 사진을 1x1 / 1x2 / 2x1 / 2x2 배치로 한 장에 합성

**추가 비용 0원**이 제약 조건이므로, 서버·클라우드 AI API를 일절 쓰지 않고 **브라우저 안에서 전부 처리하는 정적 웹앱**으로 만든다. 사진이 기기 밖으로 나가지 않으므로 개인정보 보호 측면에서도 유리하다.

### 확정된 요구사항

| 항목 | 결정 |
|---|---|
| 실행 환경 | 반응형 웹앱 (스마트폰 + 사무실 PC 겸용) |
| 배경 제거 | AI 자동 + 수동 보정 브러시 |
| 개인정보 블러 | 수동 영역 지정만 (자동 감지 없음) |
| 결과물 | 파일 다운로드만 (서버 저장 없음) |
| 작업 흐름 | 배치 먼저 선택 → 칸별 촬영 → 자유 재배치 |
| AI 모델 | 기본 Silueta(43MB), 설정에서 IS-Net 고품질(~170MB) 전환 |
| 배경 채움 | 흰색(JPG) / 투명(PNG) 모두 지원 |

---

## 비용 검증 결과 (핵심 리스크였음)

배경 제거 라이브러리·모델은 "무료처럼 보이지만 상업/공공 이용 시 유료"인 것이 많아 실제 라이선스를 확인했다.

**배제한 것**
- `@imgly/background-removal` — **AGPL-3.0**. 상업 이용은 IMG.LY와 별도 계약 필요. 관공서 업무용은 리스크가 커서 제외.
- **BRIA RMBG-1.4 / 2.0** — 비상업용 라이선스. 상업 이용은 BRIA와 계약 필요. 품질은 최상급이지만 제외.

**채택한 것 (전부 Apache-2.0 / MIT, 상업·공공 이용 무료)**
- `onnxruntime-web` (MIT) — 브라우저 추론 엔진
- **Silueta** (43MB) — U²-Net 경량화판, 기본 모델
- **IS-Net general-use** (~170MB) — Xuebin Qin, Apache-2.0, 고품질 옵션

**호스팅 주의**: Vercel Hobby 플랜은 약관상 **상업적 사용을 금지**한다. 관공서/업무용이라면 **Cloudflare Pages 무료 플랜**(대역폭 무제한, 상업 이용 허용)이나 사내 서버 배포를 쓴다. 이 부분을 놓치면 나중에 유료 전환 압박을 받는다.

---

## 기술 스택

- **Next.js 15 (App Router) + TypeScript + Tailwind CSS**, `output: 'export'`로 정적 빌드
- **onnxruntime-web** — WebGPU 우선, 미지원 시 WASM 폴백 (`executionProviders: ['webgpu', 'wasm']`)
- **Web Worker**에서 AI 추론 실행 (메인 스레드 UI 멈춤 방지)
- **Cache Storage API**로 모델 캐싱 — 최초 1회만 다운로드, 이후 오프라인 동작
- PWA manifest (홈 화면 추가 → 앱처럼 실행)
- 상태 관리는 zustand 정도면 충분 (또는 React Context)

서버·DB·인증 없음. 모든 처리가 브라우저 메모리에서 끝나고 결과는 다운로드된다.

---

## 설계상 중요한 결정 3가지

### 1. 입력 이미지를 긴 변 2048px로 다운스케일한다
스마트폰 원본은 4000x3000(12MP)급이다. 이걸 원본 그대로 캔버스 여러 장 + 마스크 + 실행취소 스냅샷까지 들고 있으면 **모바일 브라우저가 메모리 부족으로 죽는다**. 촬영/불러오기 직후 긴 변 2048px로 줄여서 작업한다. 2x2로 합성해도 4096px짜리 결과물이 나오므로 등록사이트 업로드용으로 충분하다.

### 2. 편집은 비파괴(non-destructive)로, 원본 픽셀 좌표계 기준으로 저장한다
각 슬롯은 `원본 이미지 + 알파 마스크 + 블러 영역 목록 + 크롭/회전` 을 따로 들고 있고, 화면에는 매번 합성해서 그린다. 그래야 "블러 지정했다가 취소", "배경 제거 다시 하기"가 가능하다.

블러 영역과 브러시 획은 **화면 좌표가 아니라 원본 픽셀 좌표로 저장**한다. 화면 크기가 폰/PC마다 다르고 확대·축소도 하기 때문에, 화면 좌표로 저장하면 나중에 위치가 어긋난다. 좌표 변환 유틸 하나(`screenToImage` / `imageToScreen`)를 만들어 모든 곳에서 재사용한다.

### 3. 블러는 최종 이미지 픽셀에 실제로 구워 넣는다
CSS 필터나 화면 오버레이로만 가리면 **원본 픽셀이 파일에 그대로 남아** 개인정보가 유출된다. 반드시 캔버스에서 해당 영역을 잘라 블러/모자이크 처리한 뒤 되그리는 방식으로 구현한다. 모자이크는 축소 후 `imageSmoothingEnabled = false`로 확대하면 복원이 불가능해 더 안전하다.

---

## 화면 구성

```
[1] 새 접수        접수번호·품목명 입력 → 배치 선택 (1x1 / 1x2 / 2x1 / 2x2)
        ↓
[2] 보드           선택한 배치의 빈 칸 표시. 칸을 탭하면 촬영/파일선택.
                   칸끼리 드래그로 순서 교체, 개별 재촬영, 삭제
        ↓
[3] 촬영           후면 카메라(모바일) / 웹캠(PC). 촬영 후 즉시 [4]로
        ↓
[4] 편집           ① 배경 제거 자동 실행 (진행률 표시)
                   ② 보정 브러시 — 지우개 / 복원, 브러시 크기 조절
                   ③ 블러 — 드래그로 사각 영역 지정, 블러/모자이크 강도
                   ④ 회전 / 크롭
        ↓
[5] 내보내기       배경색(흰색/투명), 포맷(JPG/PNG), 여백·구분선 설정
                   파일명 자동: {접수번호}_{품목명}_{YYYYMMDD}.jpg
```

---

## 파일 구조

```
camera/
├─ next.config.ts              # output:'export', ORT용 webpack 설정
├─ scripts/copy-ort-wasm.mjs   # node_modules의 ORT wasm → public/ort/ 복사 (postinstall)
├─ public/
│  ├─ ort/                     # onnxruntime wasm 바이너리
│  └─ manifest.json            # PWA
├─ src/
│  ├─ app/
│  │  ├─ page.tsx              # [1] 새 접수
│  │  ├─ board/page.tsx        # [2] 보드
│  │  ├─ edit/[slot]/page.tsx  # [4] 편집
│  │  └─ export/page.tsx       # [5] 내보내기
│  ├─ components/
│  │  ├─ CameraCapture.tsx     # getUserMedia 촬영
│  │  ├─ LayoutPicker.tsx      # 배치 선택
│  │  ├─ SlotGrid.tsx          # 보드 그리드 + 드래그 재배치
│  │  ├─ EditCanvas.tsx        # 편집 캔버스 (브러시·블러 입력 처리)
│  │  └─ BrushToolbar.tsx
│  ├─ lib/
│  │  ├─ segmentation/
│  │  │  ├─ worker.ts          # Web Worker: ORT 세션 + 추론
│  │  │  ├─ models.ts          # 모델 메타(URL, 입력크기, 정규화 파라미터)
│  │  │  ├─ preprocess.ts      # 리사이즈 → 정규화 → NCHW Float32Array
│  │  │  └─ postprocess.ts     # 출력 → min-max 정규화 → 알파 업스케일
│  │  ├─ modelCache.ts         # Cache Storage 다운로드/캐싱 + 진행률
│  │  ├─ canvas/
│  │  │  ├─ compose.ts         # 원본+마스크+블러 합성
│  │  │  ├─ blur.ts            # 영역 블러 / 모자이크 (픽셀 파괴적)
│  │  │  ├─ grid.ts            # 그리드 배치 합성
│  │  │  └─ coords.ts          # 화면↔원본 좌표 변환
│  │  └─ store.ts              # 접수 정보 + 슬롯 상태
│  └─ types/index.ts
```

---

## 배경 제거 파이프라인 (핵심 로직)

```
원본 ImageBitmap
  → 모델 입력 크기로 리사이즈 (Silueta 320x320 / IS-Net 1024x1024)
  → 0~1 정규화 후 ImageNet mean/std 적용, NCHW Float32Array로 변환
  → session.run({ [inputName]: tensor })
  → 출력 (1,1,H,W) 을 min-max 정규화 → 0~255 알파값
  → 원본 해상도로 bilinear 업스케일
  → 알파 마스크로 확정 (이후 브러시가 이 마스크를 수정)
```

- 세션 생성: `ort.InferenceSession.create(modelUrl, { executionProviders: ['webgpu', 'wasm'] })`
- `ort.env.wasm.wasmPaths = '/ort/'` 설정 필수 (안 하면 CDN을 찾다가 실패)
- 모델은 Hugging Face에서 런타임 다운로드 후 Cache Storage에 저장 — 저장소에 170MB 바이너리를 커밋하지 않기 위함
- 브러시 보정은 **마스크 캔버스를 긴 변 1024px로 유지**하고 그 위에 흰색(복원)/검정(지우기) 원을 그린다. 실행취소는 이 저해상도 마스크의 ImageData 스냅샷을 최근 20개까지 스택으로 보관 (원본 해상도로 스냅샷 뜨면 메모리가 감당 안 됨)

---

## 구현 순서

각 단계마다 **그 자체로 동작하는 결과물**이 나오도록 쪼갠다. AI가 마지막이 아니라 3단계인 이유는, 1~2단계만으로도 이미 쓸 만한 도구가 되기 때문이다.

### 1단계 — 뼈대 + 촬영 + 그리드 합성 (AI 없이 완결)
- Next.js 정적 export 프로젝트 셋업, Tailwind, PWA manifest
- 접수 정보 입력 + 배치 선택
- `getUserMedia` 촬영 (모바일 `facingMode: 'environment'`) + 파일 업로드 대체 경로
- 2048px 다운스케일
- 슬롯 그리드, 드래그 재배치, 재촬영/삭제
- 그리드 합성 → JPG 다운로드, 파일명 자동 생성

**이 시점에서 이미 "여러 각도 사진을 한 장으로 합쳐 저장"이 완성된다.**

### 2단계 — 개인정보 블러
- 드래그로 사각 영역 지정, 영역 목록 관리(추가/삭제)
- 블러 / 모자이크 전환, 강도 슬라이더
- 원본 픽셀에 실제 적용 후 합성
- 좌표 변환 유틸 정착

### 3단계 — AI 배경 제거
- Web Worker + onnxruntime-web 세션
- Silueta 모델 다운로드 + Cache Storage 캐싱 + 진행률 UI
- 전처리/후처리, 알파 마스크 적용
- 배경 흰색/투명 선택

### 4단계 — 보정 브러시 + 고품질 모델 + 마무리
- 지우개/복원 브러시, 크기 조절, 실행취소
- 설정 화면에서 IS-Net 고품질 모델 전환
- 회전/크롭
- 여백·구분선 등 합성 옵션

---

## 검증 방법

### 개발 중
```powershell
npm run dev
```
- PC 브라우저에서 `http://localhost:3000` — 웹캠으로 전 과정 확인

### 모바일 검증 (중요)
`getUserMedia`는 **HTTPS 또는 localhost에서만** 동작한다. 폰에서 `http://192.168.x.x:3000`으로 접속하면 카메라가 안 켜진다. 반드시 아래 중 하나로 확인한다.
```powershell
npx next dev --experimental-https   # 로컬 HTTPS + 폰에서 같은 와이파이로 접속
```
또는 Cloudflare Pages에 배포한 뒤 폰에서 확인.

### 기능별 확인 항목
1. **배경 제거** — 지갑, 이어폰 케이스, 우산처럼 유실물에 흔한 물건으로 테스트. 검은 물체 / 흰 배경 / 유리·투명 물체가 취약 지점이므로 반드시 포함
2. **블러 유출 검사** — 다운로드한 파일을 다시 열어 **확대해서** 원본 글자가 남아 있지 않은지 확인. 이미지 편집기에서 밝기·대비를 극단적으로 올려도 안 보여야 함
3. **합성** — 1x1 / 1x2 / 2x1 / 2x2 각각 저장해 칸 순서와 비율이 맞는지 확인
4. **모델 캐싱** — 첫 방문 후 새로고침했을 때 재다운로드 없이 즉시 뜨는지 (DevTools > Application > Cache Storage)
5. **메모리** — 모바일에서 4칸 모두 촬영·편집했을 때 크래시하지 않는지 (가장 흔한 실패 지점)
6. **오프라인** — 비행기 모드에서 앱이 뜨고 배경 제거가 되는지

### 라이선스 확인
`npx license-checker --summary`로 AGPL/GPL/비상업 라이선스가 섞여 들어오지 않았는지 최종 확인.
